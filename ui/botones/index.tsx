import styled from 'styled-components';
import Button from '@mui/material/Button';

export const OutlinedTextButton = styled(Button).attrs((props) => ({
}))
`
  border-color: red;
`

//export const OutlinedIconButton